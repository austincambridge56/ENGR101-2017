#include <stdio.h>
 int main( ) {
  unsigned int A = 60; 
  unsigned int B = 13;
 printf("The output of A AND B is: %d\n", A & B); 
         printf("The output of A OR B is: %d\n", A | B);
printf("The output of A XOR B is: %d\n", A ^ B);  
return 0;
 }

#include <stdio.h>
 int main( ) {
  unsigned int A = 60; 
  unsigned int B = 13;
 printf("bit shift to left of A is:%d\n", A << 4);
	 printf("bit shift to left of B is: %d\n", B >> 2);
       
return 0;
 }
